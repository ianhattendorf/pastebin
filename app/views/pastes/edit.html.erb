<h1>Edit paste</h1>
<%= form_for(@paste) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.submit 'Save changes' %>
<% end %>
<%= link_to 'back', @paste %>
